name: Retired - duplicate production diagnostics publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED_DUPLICATE_WORKFLOW=YES'
