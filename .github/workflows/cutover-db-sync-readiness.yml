name: Retired - cutover DB sync readiness

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED_CUTOVER_WORKFLOW=YES'
