name: Retired - cutover DB sync execution

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED_CUTOVER_WORKFLOW=YES'
