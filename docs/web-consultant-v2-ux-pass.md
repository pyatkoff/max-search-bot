# Web Consultant V2 UX pass

This pass reduces friction on the first interaction without changing transport, AI, manager routing, lead destinations, or analytics.

- quick actions send immediately instead of only filling the composer
- Escape closes the desktop dialog
- focus is restored after closing
- composer height resets after send/close
- quick actions are guarded while a request is already in progress
