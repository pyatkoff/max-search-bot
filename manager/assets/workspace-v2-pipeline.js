(function(){
const W=window.WorkspaceV2,{S,$,esc,pipe}=W;let bound=false,outcomeSaving=false;
function activeFilterCount(){return Number(!!S.leadStageFilter)+Number(!!Number(S.leadTagFilter||0))+Number(!!S.leadOutcomeFilter)+Number(!!S.leadTaskFilter)}
function syncTaskShortcuts(){document.querySelectorAll('.taskShortcut[data-task-filter]').forEach(button=>{const active=String(S.leadTaskFilter||'')===String(button.dataset.taskFilter||'');button.classList.toggle('active',active);button.setAttribute('aria-pressed',String(active))})}
function syncFilterUi(){const count=activeFilterCount(),badge=$('filtersCount'),panel=$('filtersPanel'),toggle=$('filtersToggle');if(badge){badge.textContent=String(count);badge.classList.toggle('hidden',count===0)}if(toggle)toggle.setAttribute('aria-label',count?`Фильтры, выбрано: ${count}`:'Фильтры');if(panel&&toggle)toggle.setAttribute('aria-expanded',String(!panel.classList.contains('hidden')));syncTaskShortcuts()}
function renderFilters(){const stage=$('leadStageFilter'),tag=$('leadTagFilter'),outcome=$('leadOutcomeFilter'),task=$('leadTaskFilter');stage.innerHTML='<option value="">Все этапы</option>'+S.pipeline.stages.map(s=>`<option value="${esc(s.stage_key)}">${esc(s.display_name)}</option>`).join('');tag.innerHTML='<option value="0">Все теги</option>'+S.pipeline.tags.map(t=>`<option value="${Number(t.id)}">${esc(t.display_name)}</option>`).join('');outcome.innerHTML='<option value="">Все исходы</option>'+Object.entries(S.pipeline.outcomes).map(([k,v])=>`<option value="${esc(k)}">${esc(v)}</option>`).join('');stage.value=S.leadStageFilter;tag.value=String(S.leadTagFilter);outcome.value=S.leadOutcomeFilter;task.value=S.leadTaskFilter;syncFilterUi()}
async function applyFilters(){syncFilterUi();await window.WorkspaceV2Inbox.load({preserveScroll:false})}
async function setTaskShortcut(value){const next=String(S.leadTaskFilter||'')===String(value||'')?'':String(value||'');S.leadTaskFilter=next;const task=$('leadTaskFilter');if(task)task.value=next;await applyFilters()}
function bindFilters(){if(bound)return;bound=true;$('leadStageFilter').onchange=async()=>{S.leadStageFilter=$('leadStageFilter').value;await applyFilters()};$('leadTagFilter').onchange=async()=>{S.leadTagFilter=Number($('leadTagFilter').value||0);await applyFilters()};$('leadOutcomeFilter').onchange=async()=>{S.leadOutcomeFilter=$('leadOutcomeFilter').value;await applyFilters()};$('leadTaskFilter').onchange=async()=>{S.leadTaskFilter=$('leadTaskFilter').value;await applyFilters()};document.querySelectorAll('.taskShortcut[data-task-filter]').forEach(button=>button.onclick=()=>setTaskShortcut(button.dataset.taskFilter||''));$('leadSearch').oninput=()=>{clearTimeout(S.searchTimer);S.searchTimer=setTimeout(async()=>{S.leadSearch=$('leadSearch').value.trim();await window.WorkspaceV2Inbox.load({preserveScroll:false})},250)};const toggle=$('filtersToggle'),panel=$('filtersPanel'),clear=$('clearFilters');if(toggle&&panel)toggle.onclick=()=>{panel.classList.toggle('hidden');syncFilterUi()};if(clear)clear.onclick=async()=>{S.leadStageFilter='';S.leadTagFilter=0;S.leadOutcomeFilter='';S.leadTaskFilter='';renderFilters();await applyFilters()}}
async function saveTags(){const ids=[...document.querySelectorAll('#leadTags input:checked')].map(x=>Number(x.value));const j=await pipe('set_tags',{conversation_id:S.current,tag_ids:ids});if(!j.ok){alert('Не удалось сохранить теги');return}await window.WorkspaceV2Conversation?.refreshLeadData({refreshInbox:true})}
function setOutcomeSaveState(text='',kind=''){const el=$('outcomeSaveStatus');if(!el)return;el.textContent=text;el.className='outcomeSaveStatus'+(kind?' '+kind:'')}
async function saveOutcome(){if(outcomeSaving)return;const outcome=$('leadOutcome').value,closeReason=$('leadCloseReason').value,note=$('leadOutcomeNote').value.trim();if(outcome==='lost'&&!closeReason){setOutcomeSaveState('Выберите причину отказа','error');$('leadCloseReason').focus();return}const button=$('saveOutcome');outcomeSaving=true;if(button){button.disabled=true;button.textContent='Сохраняем…'}setOutcomeSaveState('Сохраняем результат…');try{const j=await pipe('set_outcome',{conversation_id:S.current,outcome,close_reason:closeReason,note});if(!j.ok){setOutcomeSaveState('Не удалось сохранить результат','error');return}await window.WorkspaceV2Conversation?.refreshLeadData({refreshInbox:true});setOutcomeSaveState('Результат сохранён','success')}finally{outcomeSaving=false;const next=$('saveOutcome');if(next){next.disabled=false;next.textContent='Сохранить результат'}}}
window.WorkspaceV2Pipeline={renderFilters,bindFilters,saveTags,saveOutcome,setOutcomeSaveState,activeFilterCount,syncFilterUi,syncTaskShortcuts,setTaskShortcut};
})();
