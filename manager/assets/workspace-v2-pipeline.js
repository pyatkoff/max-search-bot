(function(){
const W=window.WorkspaceV2,{S,$,pipe}=W;const tagSavingLeads=new Set(),stageSavingLeads=new Set();
function sameLead(target){return Number(S.current||0)===Number(target||0)}
async function refreshAfterSave(target){if(sameLead(target))return !!(await window.WorkspaceV2Conversation?.refreshLeadData({refreshInbox:true,conversationId:target}));await window.WorkspaceV2Inbox?.load({preserveScroll:true});return false}
function setSalesSaveState(text='',kind=''){const el=$('salesSaveStatus');if(!el)return;el.textContent=text;el.className='salesSaveStatus'+(kind?' '+kind:'')}
function ensureCurrentStageOption(stage,pipeline={}){if(!stage)return false;const current=String(pipeline.stage?.stage_key||'').trim();if(!current||[...stage.options].some(option=>String(option.value)===current))return false;const option=document.createElement('option');option.value=current;option.textContent=String(pipeline.stage?.display_name||current)+' (неактивен)';option.disabled=true;option.selected=true;stage.prepend(option);return true}
async function saveStage(stage,previousStage){if(!stage)return false;const target=Number(S.current||0),wanted=stage.value;if(!target)return false;if(stageSavingLeads.has(target)){if(stage.isConnected)stage.value=previousStage;if(sameLead(target))setSalesSaveState('Сохранение этапа уже выполняется','dirty');return false}stageSavingLeads.add(target);stage.disabled=true;if(sameLead(target))setSalesSaveState('Сохраняем этап…');try{const j=await pipe('set_stage',{conversation_id:target,stage_key:wanted});if(!j.ok){if(stage.isConnected)stage.value=previousStage;if(sameLead(target))setSalesSaveState('Не удалось изменить этап','error');return false}await refreshAfterSave(target);if(sameLead(target))setSalesSaveState('Этап сохранён','success');return true}catch(e){if(stage.isConnected)stage.value=previousStage;if(sameLead(target))setSalesSaveState('Не удалось изменить этап','error');return false}finally{stageSavingLeads.delete(target);if(stage.isConnected)stage.disabled=false}}
async function saveTags(changed){const target=Number(S.current||0);if(!target||tagSavingLeads.has(target))return false;const inputs=[...document.querySelectorAll('#leadTags input')],previous=changed?!changed.checked:null,ids=inputs.filter(x=>x.checked).map(x=>Number(x.value));tagSavingLeads.add(target);inputs.forEach(x=>x.disabled=true);if(sameLead(target))setSalesSaveState('Сохраняем теги…');try{const j=await pipe('set_tags',{conversation_id:target,tag_ids:ids});if(!j.ok){if(sameLead(target)&&changed?.isConnected)changed.checked=previous;if(sameLead(target))setSalesSaveState('Не удалось сохранить теги','error');return false}await refreshAfterSave(target);if(sameLead(target))setSalesSaveState('Теги сохранены','success');return true}catch(e){if(sameLead(target)&&changed?.isConnected)changed.checked=previous;if(sameLead(target))setSalesSaveState('Не удалось сохранить теги','error');return false}finally{tagSavingLeads.delete(target);inputs.forEach(x=>{if(x.isConnected)x.disabled=false})}}
function bindSalesEditor({canEdit=false,pipeline={}}={}){if(!canEdit)return;const stage=$('leadStage');if(stage){ensureCurrentStageOption(stage,pipeline);let confirmedStage=String(pipeline.stage?.stage_key||stage.value);stage.onchange=async()=>{const wanted=stage.value;if(await saveStage(stage,confirmedStage))confirmedStage=wanted}}document.querySelectorAll('#leadTags input').forEach(x=>x.onchange=()=>saveTags(x));window.WorkspaceV2Outcome?.bind({canEdit,pipeline})}
window.WorkspaceV2Pipeline={bindSalesEditor,saveStage,saveTags,setSalesSaveState,sameLead,refreshAfterSave,ensureCurrentStageOption};
})();
